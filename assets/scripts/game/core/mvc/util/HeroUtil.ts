import { _decorator, Component, Node } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('HeroUtil')
export class HeroUtil extends Component {
    start() {

    }

    update(deltaTime: number) {
        
    }
}

